apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  namespace: toy-system
spec:
  ingressClassName: nginx
  rules:
    - host: app.homelab.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80