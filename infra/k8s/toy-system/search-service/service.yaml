apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: toy-system
spec:
  selector:
    app: search-service
  ports:
    - name: http           # 加这一行，名字随便起，但要和ServiceMonitor里的port值对上
      port: 8083
      targetPort: 8083